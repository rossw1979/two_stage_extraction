# 分阶段 VLM 提取方案设计

## 问题背景

`prompt_v3_with_fewshot.md` 将提取准确率提升到可用水平，但推理耗时增加 4~5 倍。瓶颈在于：
- 输入 token 翻倍（few-shot 示例占 100+ 行）
- 强制思维链（整页预扫描 → 数行列 → 输出 → 逐项勾选校验）
- 每页无论内容复杂度如何，都走最重路径

## 核心思路：按内容复杂度动态降级

医学指南页面并非每一页都是表5那种复杂评分表。大多数页面是：
- 纯文字段落（50% 以上）
- 简单三线表（30%）
- 复杂评分表 / 跨页大表（< 20%）

分阶段调用的本质是先做一次"轻量侦察"，根据页面实际复杂度选择匹配的 prompt 策略，避免用大炮打蚊子。

---

## 三阶段流水线

```
┌─────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  阶段1: Scout │ ──→ │  阶段2: Extract │ ──→ │  阶段3: Verify  │
│  (结构侦察)    │     │  (按需提取)      │     │  (定向校验)      │
└─────────────┘     └─────────────────┘     └─────────────────┘
   极简prompt          动态选择prompt          仅校验可疑项
   输出: JSON           输出: Markdown          输出: 修正补丁
   耗时: ~20%          耗时: ~60%              耗时: ~20%
```

---

### 阶段 1 — Scout（结构侦察）

**目标**：让 VLM 只识别页面有什么，不提取内容。

**Prompt 设计原则**：
- 去掉所有格式转换规则、few-shot 示例、校验清单
- 只保留"角色定位 + 观察任务 + 输出格式"
- 强制输出 JSON，限制长度

**示例 Prompt**（约 30 行）：

```
你是一位医学文献结构分析专家。请观察这张图像，只描述页面结构，不提取具体内容。

输出要求（严格 JSON 格式）：
{
  "page_layout": "单栏|双栏|混排",
  "content_blocks": [
    {"type": "段落|表格|图片|公式|流程图", "position": "左上|右上|左下|右下|中央", "complexity": "低|中|高"}
  ],
  "tables": {
    "count": 数字,
    "max_rows_estimate": 数字,
    "has_merged_cells": true|false,
    "has_multiline_cells": true|false,
    "has_footnote_below": true|false
  },
  "recommendation": "使用轻量提取|使用标准提取|使用完整提取"
}

判断标准：
- 轻量提取：纯文字段落，无表格或只有简单2~3列表格
- 标准提取：有表格但无复杂合并单元格、无评分尺
- 完整提取：存在风险评分表（GRACE/DAPT/PRECISE-DAPT等）、跨页表格、或大量合并单元格
```

**为什么快**：
- 输入 prompt 只有 30 行，无 few-shot
- 输出被强制为简短 JSON，通常 < 200 token
- 不需要逐字核对，不触发 CoT 深度推理

---

### 阶段 2 — Extract（按需提取）

根据 Scout 返回的 `recommendation` 字段，分支到三种策略：

#### 策略 A：轻量提取（纯文字页）

适用场景：章节前言、方法学描述、讨论段落。

Prompt 特征：
- 保留角色定位、排版顺序、内容过滤规则
- **去掉**表格三步法、表格周边文字检查、页面四角检查
- **去掉** few-shot 示例
- **去掉** 12 项 checkbox 校验清单，只保留 3 项：无跨栏拼接、无数值幻觉、无遗漏引用上标

预估耗时：v3 完整版的 20~30%

#### 策略 B：标准提取（普通表格页）

适用场景：一般数据表格（三五行、无复杂单元格）。

Prompt 特征：
- 保留表格基本规则（Markdown 格式、不合并单元格）
- **去掉** few-shot 示例
- **去掉**评分表特殊要求
- 保留 5 项 checkbox（结构、行列、数值、引用、HTML）

预估耗时：v3 完整版的 50~60%

#### 策略 C：完整提取（复杂评分表 / 跨页表）

适用场景：表5（DAPT/PRECISE-DAPT）、GRACE 评分表、跨页大表。

Prompt 特征：
- 使用 `prompt_v3_with_fewshot.md` 完整版
- 包含 few-shot 示例、评分表特殊要求、12 项 checkbox

预估耗时：与当前 v3 持平

**关键收益**：只有 < 20% 的页面走完整路径，整体平均耗时大幅下降。

---

### 阶段 3 — Verify（定向校验）

**目标**：不重新提取，只让 VLM 做"找茬"。

**触发条件**（满足任一即触发）：
- Scout 报告 `has_footnote_below: true` 但 Extract 输出未找到注释放置区
- Scout 报告表格行数 N，Extract 输出表格行数 ≠ N
- Extract 输出中出现 `<div>`、`<span>` 等被禁止的标签
- 页面为双栏混排，Extract 输出出现明显跨栏拼接语义

**Prompt 设计**：

```
你是一位医学文献校验专家。下方是某页面的 VLM 提取结果，请你对照原图检查以下特定问题：

[在此处插入阶段1的Scout结构和阶段2的Extract输出]

请仅回答以下问题（用 JSON 格式）：
{
  "issues": [
    {"location": "表格第X行", "problem": "遗漏了末尾行", "severity": "高|中|低"}
  ],
  "needs_re_extract": true|false,
  "re_extract_focus": "需要重点修正的部分描述"
}

注意：
- 只列出你确认存在的问题，不要猜测
- 如果提取结果与原图基本一致，issues 留空
```

**修正策略**：
- 如果 `needs_re_extract: false` → 直接采纳阶段 2 结果
- 如果 `needs_re_extract: true` → 用"定向精修 prompt"重新提取，prompt 中会明确告诉模型"上次遗漏了 XX，请重点检查"

**为什么快**：
- Verify 的输入是文本（ Scout JSON + Extract Markdown），不需要重新编码整张高清图片
- 或者即使给原图，任务范围被严格限定为"找这 3~5 个问题"

---

## 实施细节

### 1. 复杂度判断的准确率

Scout 阶段用 VLM 自身判断复杂度，存在误判可能。保险做法：
- **保守策略**：只要 Scout 报告存在表格，就至少走"标准提取"
- **关键词兜底**：如果 OCR 预扫描（可用 pdfplumber 快速跑一遍）发现 "PRECISE-DAPT"、"GRACE"、"CRUSADE"、"DAPT 评分" 等关键词，强制走"完整提取"

### 2. 调用次数与成本权衡

| 方案 | 调用次数 | 单次 token 量 | 总耗时（相对） |
|------|---------|-------------|--------------|
| 单阶段 v3 完整 | 1 | 极大 | 100% |
| 三阶段（无问题） | 2 | 小 + 中 | ~40% |
| 三阶段（需修正） | 3 | 小 + 中 + 小 | ~70% |

结论：即使最坏的 3 次调用场景，总耗时仍低于单阶段完整提取（因为 Verify 输入是文本，且任务聚焦）。

### 3. 可并行的优化

阶段 1 的 Scout 可以对全量页面批量并发执行（因为 prompt 轻量），得到全书的"内容地图"。然后：
- 纯文字页 → 批量走轻量提取
- 表格页 → 逐个走标准/完整提取

这样可以把大量简单页面的处理时间压到最低。

---

## 最小可运行版本（两阶段简化）

如果三阶段觉得复杂，可以先做一个"两阶段简化版"：

```
阶段1（侦察）: 极简 prompt → 判断是否需要 few-shot
阶段2（提取）: 如果不需要 few-shot，用 prompt_v3 去掉示例版；如果需要，用完整版
```

这个版本只比原来多一次轻量调用，但能让 80% 的页面走轻量路径，整体速度提升 3 倍左右。
